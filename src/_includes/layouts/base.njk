<!doctype html>
<html lang="{{ site.lang }}">
<head>
{% include "partials/head.njk" %}
</head>
<body class="cp-body">

  <a class="cp-skip-link" href="#inhalt">Zum Inhalt springen</a>

  {# Kopfbereich und <main> werden in index.njk gesetzt: hero.njk ist
     der <header> der Seite und muss AUSSERHALB von <main> stehen,
     damit der Sprunglink den Kopfbereich überspringt. #}
  {{ content | safe }}

  {% include "partials/footer.njk" %}

</body>
</html>
